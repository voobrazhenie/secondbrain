{
  "name": "secondbrain",
  "private": true,
  "type": "module",
  "description": "Second Brain is a set of static pages — no build step and no runtime dependencies. Everything here is tooling: node's own test runner, and Playwright, which drives the real pages in a real browser against a stubbed Firebase.",
  "scripts": {
    "test": "node --test exercise/*.test.mjs tests/*.test.mjs",
    "test:unit": "node --test exercise/*.test.mjs",
    "test:browser": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
